package playground.parts.two

import kotlinx.coroutines.delay

/**
 * Part 2 of our Kotlin playground focusing on functions.
 *
 * To see function documentation, declare function associated to specified documentation.
 *
 * @see [playground.parts.one.Variables].
 */
class Functions {

    /**
     * Takes two [Int] parameters and returns their sum.
     *
     * @param first The first [Int] parameter
     * @param second The second [Int] parameter
     */


    /**
     * After a delay for a specified time using [delay], run a block of code
     * using a callback.
     *
     * Visit [composing suspending functions](https://kotlinlang.org/docs/reference/coroutines/composing-suspending-functions.html)
     * for more information in regards to using the [delay] function.
     *
     * Example:
     * ```
     * setTimeout(2000) {
     *  println("Sample callback called after 2000ms")
     * }
     * ```
     *
     * @param time [Long] used for [delay] function
     * @param callback encapsulates block of code to run after specified time
     */

}